/**
 * Problema: [Nome do Problema]
 * Plataforma: [Beecrowd/Exercism]
 * Dificuldade: [Fácil/Médio/Difícil]
 * Categoria: [Categoria do problema]
 * 
 * Descrição:
 * [Breve descrição do problema]
 * 
 * Entrada:
 * [Formato da entrada]
 * 
 * Saída:
 * [Formato da saída]
 * 
 * Complexidade:
 * Tempo: O(?)
 * Espaço: O(?)
 */

// Importar readline para entrada de dados (se necessário)
const readline = require('readline');

const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});

// Função principal
function solve() {
    // Implementar solução aqui
}

// Leitura de entrada
rl.on('line', (input) => {
    // Processar entrada
    console.log(solve(input));
    rl.close();
});